// V2 wrapper — renders the original Home page inside V2Layout (AppLayout is transparent in V2 mode)
import OriginalHome from '@/pages/Home';
export default function HomeV2() { return <OriginalHome />; }
